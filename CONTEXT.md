# Context: e2m2e

Earth to Moon, Moon to Earth — 用于地月空间轨道与转移轨道设计的 Python 库。

## 核心使命

e2m2e 为地月空间任务提供**轨道设计相关的计算基础设施**：
- 建立天体系统的动力学模型；
- 生成并精化周期/拟周期轨道；
- 在轨道之间构造转移轨迹；
- 将 CR3BP 轨道转换到星历模型；
- 可视化计算结果以辅助分析与决策。

## 系统边界

e2m2e **不处理**：
- 航天器详细工程（质量、推力器配置、电源、热控）。
- 任务规划的时间调度（发射窗口、地面站可见性）。
- 姿轨控（GNC）闭环仿真。
- 通用 N 体积分问题（仅聚焦轨道设计相关算法与数据流）。

## 领域术语

### 系统与环境

**动力学系统（System）** — 天体的几何、引力与运动学模型，是后续一切计算的上下文。System 抽象基类（`abc.ABC`）定义最小接口：
- `frame`（`ReferenceFrame`）— 坐标系标识枚举（如 `SYNODIC`、`INERTIAL`），仅作标签、不做变换。
- `coordinate_system`（`CoordinateSystem | None`，可选）— 变换对象（`坐标轴`+`原点`）；`EphemerisSystem` 有，`CR3BP_System` 无（走 `CoordinateTransformation`），`System.transform()` 薄委托给它。
- `unit_system`（`UnitSystem`）— 单位系统。
- `gravitational_parameter(body)` — 天体引力参数；CR3BP 返回无量纲，星历返回 km³/s²。
_Avoid_: 系统模型、动力学模型

**CR3BP 系统（CR3BP System）** — `System` 的实现，描述圆型限制性三体问题：两个主天体绕公共质心作圆周运动，第三体质量可忽略。采用以质心为原点的旋转坐标系，所有量无量纲化（DU, TU, VU）。在该会合旋转系中运动按**相位**演化——**自治系统**，与历元无关（给定初始相位，后续状态唯一确定）。特有概念：`LibrationPoint`（平动点）。
_Avoid_: 三体系统、无量纲系统

**星历系统（Ephemeris System）** — `System` 的实现，基于 SPICE 内核查询天体星历。采用 J2000 等惯性坐标系，单位为物理单位（km, s, km/s）。运动按**历元**演化——**非自治**，天体位置随历元变化（由 SPICE 查询确定）。不定义平动点。
_Avoid_: 星历数据源、目标天体历表

**坐标轴（`Axes`）** — 坐标系的**朝向**部分。`rotation_matrix(et)` 给出到 ICRF/J2000 的旋转矩阵 R（约定 `r_icrf = R @ r_axes`），`rotation_and_rate` 给出 (R, Rdot)（`v_icrf = R @ v_axes + Rdot @ r_axes`）。实现按精度分档：`ITRFSpiceAxes`（SPICE ITRF93，高精度）、`GMATITRFAxes`（GMAT 兼容原生）、`ITRFApproxAxes`（低精度教学）、`ICRSAxes`（单位阵）、`IAU2000EqAxes`。
_Avoid_: 坐标系（指 `CoordinateSystem` 整体，非朝向部分）

**原点（`Origin`）** — 坐标系的**平移**部分。`state(et)` 返回该原点在 ICRF 中的绝对状态 [r, v]。子类 `CelestialBodyOrigin`（天体中心）、`InertialOrigin`（质心等无平移点）。
_Avoid_: 物理天体本身（原点是参考点）

**坐标系（`CoordinateSystem`）** — `坐标轴`（朝向）+ `原点`（平移）的组合体，是向量/状态变换的单位（`transform_vector`/`transform_state`）。与 `单位系统` 正交；`System.coordinate_system` 持有一个供力模型变换。注：`CR3BP_System` **不**经 `CoordinateSystem` 变换——会合旋转系/惯性系/天体中心系转换走独立的 `CoordinateTransformation`（`e2m2e/core/coordinate.py`，绑 `CR3BP_System`），故其 `coordinate_system` 为 `None`；会合系原则上可表达为（会合 `Axes`+质心 `Origin`），但代码走 legacy 路径。
_Avoid_: 坐标框架（口语同义词，规范用"坐标系"）；把 `frame` 枚举当坐标系（仅标签，不做变换）

**单位系统（Unit System）** — 描述数值的量纲，如 CR3BP 无量纲单位、SI 单位。同一坐标系中的状态可用不同单位系统表示。
_Avoid_: 单位、量纲

**平动点（Libration Point）** — CR3BP 中旋转坐标系下的五个平衡解：L1、L2、L3 共线，L4、L5 三角。仅属于 CR3BP System 的概念。
_Avoid_: 拉格朗日点（虽然是同义词，但在 e2m2e 中用"平动点"）

### 状态与轨道

**状态向量（State Vector）** — 航天器的瞬时状态，六元素数组 `[x, y, z, vx, vy, vz]`，顺序固定不变。
_Avoid_: state（单独使用含混）

**状态（State）** — `State Vector` 在某一时刻的实例。在 e2m2e 中保持为裸 numpy 数组，形状 `(6,)` 或作为 `Orbit.states` 中的一行。
_Avoid_: 状态向量（当指单点时）

**单条轨道（Orbit）** — 航天器状态随时间演化的轨迹容器。持有 `states`（形状 `(n_points, 6)`）、`times`（形状 `(n_points,)`）和 `system`。坐标系与单位由绑定的 `system` 解释。`Orbit` 不自带周期、Jacobi 常数、稳定性等派生属性，这些由外部算法按需计算。
_Avoid_: 单个 JSON、种子轨道（除非确实用于延拓）

**轨道族（Orbit Family）** — 同一类型、由连续参数（如 Jacobi 常数、振幅）索引的一组 `Orbit` 的集合。类型由 `OrbitFamilyType` 枚举标记。
_Avoid_: 采样轨道、代表性轨道集

**CR3BP 周期轨道族类型**

| 族名 | 英文名 | 相关平动点 | 物理特征 | 实现状态 |
|------|--------|------------|----------|----------|
| Lyapunov | Lyapunov Family | L1, L2, L3 | 平面周期轨道，沿共线平动点主轴振荡 | 已实现 |
| Halo | Halo Family (North/South) | L1, L2 | 三维周期轨道，稳定性指数接近 1 的高振幅区域通常称为近直线晕轨道 (NRHO)；L3 处无经典 Halo 轨道族 | 已实现 |
| Vertical | Vertical Family | L1–L5 | 垂直方向振荡的周期轨道 | 已实现 |
| Axial | Axial Family | L1–L5 | 沿平动点轴向的周期轨道 | 待实现 |
| Butterfly | Butterfly Family | L1–L2 | 连接两个共线平动点的对称轨道 | 已实现 |
| DRO | Distant Retrograde Orbit | secondary | 围绕次天体的远程逆行轨道 | 已实现 |
| DPO | Direct Prograde Orbit | secondary | 围绕次天体的顺行轨道 | 待实现 |
| SPO | Short Period Orbit | L4, L5 | 三角平动点附近的短周期轨道 | 待实现 |
| LPO | Long Period Orbit | L4, L5 | 三角平动点附近的长周期轨道 | 待实现 |
| Tadpole | Tadpole Orbits | L4, L5 | 围绕单个三角平动点的蝌蚪形轨道 | 待实现 |
| Horseshoe | Horseshoe Orbits | L4–L5 | 跨越两个三角平动点的马蹄形轨道 | 待实现 |
| RO | Resonant Orbit（共振轨道） | 全系统 | 满足 m:n 共振比例的周期轨道，如 3:1（周期是月球周期的 3 倍）、3:2（1.5 倍），通过共振比区分 | 已实现 |
| Dragonfly | Dragonfly Family | L1–L2 | 连接两个共线平动点的非对称轨道 | 已实现 |

**周期** — 一条 **单条轨道** 在 CR3BP 模型中完成一次重复运动所需的时间。周期可以来自用户输入、参考初值、轨道修正结果或输出诊断。周期不同于采样点数、传播时长。
_Avoid_: 采样点数、任意传播时长、周期倍数

### 力模型与传播

**力模型（`PhysicalModel`）** — 对航天器加速度的一项物理贡献（`PhysicalModel` 子类：引力场、大气阻力、太阳光压、有限推力等）。每个力模型接收 `(t, state, system)`，返回在 `system.coordinate_system`（传播坐标系）下的加速度 `(3,)`。需要中间坐标系（如 ITRF 算阻力、ICRF 算光压）的力模型**自行**经由 `system.transform` 完成变换并转回传播系。
_Avoid_: 力（单独使用含混）、force（同上）

**力模型聚合（`ForceModel`）** — 多个 **力模型** 加速度的合成，由 `ForceModel`（`Dynamics` 子类）承担。聚合时容器对各力模型返回的加速度做矢量求和；**坐标变换的职责属于每个力模型，不属于容器**。容器不代劳变换、不感知各力模型的中间坐标系。
_Avoid_: 容器协调坐标转换（误导：暗示容器代劳变换）、力叠加（口语化）

**力模型配置（Force Model Config）** — 一组 `力模型` 的清单，写成 JSON 或 dict
（是数据，不是代码）。每条写明一个力是什么、参数取多少、叫什么名字、是否启用。
`ForceModel` 读这份清单就能构建出来，不必逐条写 Python 调 `add_force`。配置只
记力本身，不记初始状态或传播时长——那是运行时才给的。配置能存盘、也能原样读回，
读回再构建所得的力应与原先相同；因此凡不能写成数据的输入（如手写的 Python 函数）
配置管不了，`FiniteBurn` 的推力曲线与方向只接受几种固定写法。
_Avoid_: 把配置当作运行规格（它只列力，不驱动传播）；把字段名、版本号这类序列化
细节当作领域概念来记（那是实现，归代码与 ADR）

### 推进与转移

**推进模型（Propulsion Model）** — 描述航天器如何改变速度的策略。`Transfer` 持有推进模型以支持不同转移类型。_仅用于 `Transfer` 的出发/到达代价建模（`alpha/beta` 速度分解），与传播中的推力事件不同；参见 推力/机动模型_。
- `ImpulsivePropulsion`：脉冲机动，速度瞬时变化。将出发速度分解为切向与法向分量：``v = alpha * |v| * t_hat + beta * |v| * n_hat``，其中 `alpha` 为切向速度比，`beta` 为法向速度比（默认 `0.0`，即纯切向）。
- `LowThrustPropulsion`：连续小推力（待实现）。
_Avoid_: 推进方式、动力模型

**推力/机动模型（Thrust / Maneuver Model）** — 航天器在传播中改变速度的两种事件，定义在 `e2m2e.core.forces.thrust`。与 **推进模型**（转移代价建模）正交。
- `FiniteBurn(PhysicalModel)`：连续推力加速度。`thrust_profile(t)` 返回推力大小（N，标量，`0` 表示关机）；`direction` 接受固定向量 `(3,)` 或 `(t, state) -> (3,)` 可调用（传播坐标系内，内部归一化）；`mass` 常量（kg）。返回 `thrust/mass · d̂ / 1000`（km/s²）。合并了 GMAT R2026a 的 `FiniteBurn`（配置）与 `FiniteThrust`（力模型），未引入 `Thruster` 硬件层。
- `ImpulsiveBurn`：瞬时 Δv 机动事件 `(epoch, delta_v)`（frozen dataclass），**不继承 `PhysicalModel`**；由 `ForceModel.propagate_maneuvers` 在 `epoch` 处中断传播、在传播坐标系内施加 `state[3:6] += delta_v`、续传。输出在 burn epoch 处为 post-burn 单行（无重复 epoch），`BurnApplication` 记录 `velocity_before/after`。
- 第一阶段：Δv 与方向均在传播（惯性）坐标系内解释，不做坐标变换；VNB/LVLH burn 坐标系推迟到 Slice 12（届时加 `frame` 字段，转换走 `CoordinateSystem.transform_vector`，对应 GMAT `Burn::ConvertDeltaVToInertial` 的 `coincident=true` 纯旋转）。
- 质量变化：不支持（`mass` 常量）。GMAT 通过把质量作为增广状态通道实现（脉冲用齐奥尔科夫斯基方程离散扣、有限推力写 `dm/dt`），e2m2e 状态（REQ-002）为 6 维无质量通道，故推迟到独立切片。
_Avoid_: 推进方式（与 Propulsion Model 混淆）、burn（单独使用含混，须带限定：脉冲/有限）

**转移（Transfer）** — 从出发条件到到达条件的轨迹。出发与到达由 `TerminalCondition` 定义，不由 `Orbit` 强制限定。
_Avoid_: 轨道族转换、星历转换

**端点条件（Terminal Condition）** — 转移出发或到达必须满足的条件。
- `OrbitTerminal`：位于某条 `Orbit` 上，出发状态取轨道首点，到达状态通过动力学传播获取。
- `StateTerminal`：固定状态与时间，出发与到达状态均为固定值，不依赖动力学传播。已实现，但尚未接入 DRO→RO 优化器。
- `LEOTerminal`、`GEOTerminal`：近地/地球同步停泊轨道端点（待实现）。
- `BodySurfaceTerminal`：位于天体表面某点（待实现）。
_Avoid_: 出发轨道、到达点（过于笼统）

**Δv（Delta-v）** — 速度增量，衡量转移所需的速度变化总量。脉冲模型中为各脉冲大小之和。
_Avoid_: 推力、燃料消耗

### 算法

**动力学（Dynamics）** — 在给定 `System` 上积分运动方程、返回状态历史的对象（ADR 0001）。**不同 `动力学` 的根本差别在于坐标系**：`System` 决定坐标系——`CR3BP 系统`（地月会合旋转系，运动按**相位**演化、自治、与历元无关）或 `星历系统`（高精度惯性系，运动按**历元**演化、天体位置由 SPICE 查询）。**力在坐标系之上声明、与之正交**：统一模式下每个 `Dynamics` = `System` + 可选声明 `力模型` 列表；力影响外推计算量与保真度，不决定模型身份。已实现：`CR3BP_Dynamics`（会合系，引力用解析的伪势与科氏力表达）、`EphemerisDynamics`（惯性系，引力用解析的 N 体公式表达）、`ForceModel`（`力模型聚合`，惯性系；引力也作为力模型处理、与其他力一起求和，不单独解析）。**会合系 + 声明力**（受摄 CR3BP `中间模型`）待建。因 `力模型` 接口不提供 ∂a/∂r，携带声明力的 `动力学` 暂不支持解析 STM；加入时变力（如太阳引力）后，会合系方程显式含时间，变为时间周期系统（以太阳的会合周期为周期），不再自治。
_Avoid_: 求解器；把“解析 vs 力分解”当作根本差别（根本差别在坐标系）；把 `ForceModel` 视作"坐标系+力"模式之外的特例（它是惯性系下的一种实现）

**中间模型（Intermediate Model）** — 在基础模型（`CR3BP 系统` 或 `星历系统`）之上声明摄动 `力模型` 构成的模型，**不是第三种基础 `System`**。如会合旋转系 + 太阳引力 + SRP。加入时变力（太阳在会合系中运动）后，方程显式含时间，变为时间周期系统，不再自治。当前未实现（待按 `动力学` 统一模式给解析类加可选力列表，并解决旋转系下 `力模型` 的 `require_inertial_frame` 约束）。
_Avoid_: 把中间模型当作独立的第三种 System；把"CR3BP + 力"仍当作自治 CR3BP

**积分器（Integrator）** — `动力学` 调用的底层数值推进引擎，实现在 Rust crate（`crates/e2m2e-integrators`）并经 `e2m2e.integrators` 公开。共性：不持有 `System` 上下文、不做事件检测、不控制传播全程（那些是 `动力学` 的职责）；每次调用推进一步并返回误差估计与步长建议。分三族：
- **单步积分器（Single-step / Runge-Kutta 族）**：无状态、自适应步长、积一阶 `y'=f(t,y)`、输出全状态。`rk_step`，方法 `Pd45`/`Pd78`/`Rk89`。
- **多步积分器（Multistep / Adams 族）**：携带**历史缓冲**（纯导数采样）、固定步长、积一阶 `y'=f`、输出全状态。`multistep_step`，方法 `Abm`。
- **二阶积分器（Second-order / Cowell 族）**：携带**历史缓冲**（位置+加速度采样，采样格式与 Adams 不兼容）、固定步长、积二阶 `x''=a(t,x)`、输出**仅位置**（速度需有限差分恢复）。`cowell_step`。
_Avoid_: 步进器（overloaded；中文中可接受"步进器"作为实现层面同义词）；把多步/二阶排除在"积分器"外（与 GMAT 分类、crate 目录相悖）

**历史缓冲（history）** — 多步与二阶积分器跨步保留的既往采样（单步积分器无此物）。固定步长；改步长须重建。两种**不兼容**的采样格式：**Adams 族**为纯导数采样 `[f_{n-k}, …, f_n]`；**Cowell 族**为位置+加速度混合采样（如 `[x_{n-1}, x_n, a_{n-7}, …, a_n]`），故两者不能共用同一缓冲。Cowell 的混合采样格式偏离 #107 grilling 共识（当时设想统一纯导数采样格式），记录于 issue 与 `cowell.rs` 注释。
_Avoid_: 把 Adams 与 Cowell 的 history 视为同一种采样格式

**状态转移矩阵（STM / State Transition Matrix）** — 状态扰动的线性传播矩阵，形状 `(n_points, 6, 6)`。由 `Dynamics.propagate(..., with_stm=True)` 返回。
_Avoid_: 雅可比矩阵（STM 是状态空间的雅可比，但与约束雅可比不同）

**微分修正（Differential Correction）** — 单点初值修正算法。通过调整单个初始状态的自由分量，使后续轨迹满足约束条件（如周期性、对称性）。
_Avoid_: 优化、迭代求解

**修正策略（Correction Strategy）** — 微分修正的配置：定义固定参数、自由变量、目标约束及其权重。不同轨道族或对称性对应不同策略。
_Avoid_: 配置、参数

**多重打靶（Multiple Shooting）** — 将长轨迹拆分为多段，在每段端点施加连续性约束，从而降低积分敏感性、提高收敛性的边界值求解器。
_Avoid_: 分段积分、拼接

**延拓（Continuation）** — 从一条已知轨道、参考初值或已收敛解出发，沿某个轨道族参数方向逐步生成相邻轨道的过程。延拓通常用于 **生成轨道族**；它描述生成方法，不等同于 **轨道族** 本身。
_Avoid_: 轨道族本身、批量运行

**稳定性分析（Stability Analysis）** — 计算单周期轨道单值矩阵的特征值谱，以分类轨道稳定性。属于外部算法，结果不写入 `Orbit`。
_Avoid_: 特征值计算、稳定性判断

**初始猜测策略（Initial Guess Strategy）** — 为特定轨道族生成粗糙 `Orbit` 的策略函数，作为微分修正或延拓的输入。例如 Halo 轨道的三阶解析近似。
_Avoid_: 种子、参考轨道

### 星历转换

**星历转换** — 将 CR3BP 轨道转换到星历模型的过程。星历转换不同于 **转移设计**：前者解决动力学模型转换问题，后者解决任务连接与设计问题。
_Avoid_: 转移设计、轨道族转换

**轨道族转换** — 将轨道族内每条轨道逐条独立转换到星历模型；单条转换失败时默认记录该项并继续处理其余轨道，可由用户选择快速失败。
_Avoid_: 沿族递推、只转换里程碑轨道

**星历转换输入文件** — 用户显式提供给转换工具的 CR3BP 轨道 JSON 文件；工具不应隐式选择最新文件。
_Avoid_: 最新输出文件、默认输入文件

**轨道族转换结果** — 一次 **轨道族转换** 产生的单个汇总 JSON，包含每条轨道的转换状态、诊断信息和成功时的星历轨迹。
_Avoid_: 每轨道分散输出、只保存成功项

**星历转换方法** — 星历转换工具用于构造并修正星历模型中轨迹表示的算法选择。对于需要分段表示的情况，方法用于修正不同 **patch points** 对应的 **轨道初值**，使每段外推到下一个 patch point 时位置和速度满足连续性要求；对于周期性强、三体摄动影响小且不拆分的轨道，方法可用于构造和修正轨道初值。
_Avoid_: 星历数据源、目标天体历表、固定不可选算法

**参考历元** — 用户为星历转换显式提供的 UTC 时间基准，用于将 CR3BP 轨道的初始状态、轨道初值或 **patch points** 映射到 J2000 星历时间。相同的 **星历转换输入文件** 在不同参考历元下可能得到不同星历转换结果。
_Avoid_: 隐式默认历元、轨道周期、普通运行时间

**待转换轨道** — 星历转换从 **星历转换输入文件** 中读取并准备转换到星历模型的 **单条轨道**。当输入文件包含 **轨道族** 时，轨道族中的每条单条轨道都是一个待转换轨道，并按轨道族转换语义逐条独立转换。
_Avoid_: 输入文件本身、最新输出文件、轨道族整体作为一个不可拆分对象

**轨道初值** — 用于开始轨道传播或数值修正的一组初始状态量，通常包含位置和速度。轨道初值属于 **初始条件** 的一部分。
_Avoid_: 参考数据集、完整初始条件、生成结果、轨道族

**初始条件** — 用户为生成轨道提供的起点信息，用于确定待生成轨道的初始状态或目标特征。初始条件可以来自手动输入，也可以来自 **参考初值**。
_Avoid_: 只指手动输入、排除参考初值、生成结果

**参考初值 (Reference Initial Condition)** — 用户从 **参考数据集** 中选择的一条轨道记录，或按 Jacobi 常数等条件匹配得到的一组初始条件，用作生成工具的起点。启用参考初值后，与它对应的手动初始条件输入应互斥。
_Avoid_: 参考数据集（数据集合层级）、Catalog 种子、生成结果、手动输入值

**参考数据集 (Reference Catalog)** — 项目提供或用户指定的轨道参考数据集合，包含可供生成工具使用的轨道记录。
_Avoid_: 参考目录（容易误解为文件夹目录）、数据库（容易误解为联网或 SQL 数据库）、模板

**patch points** — 分段星历转换中用于表示轨迹段之间连接关系，并进行连续性修正的轨迹连接点。不是所有星历转换都需要 patch points；对于不拆分的轨道，工具可以只构造和修正轨道初值。
_Avoid_: 所有星历转换都必须有的点、轨道初值、采样点、标注点、目标点

**容差** — 工具用于判断匹配、收敛、连续性或误差是否可接受的数值阈值。容差通常属于高级参数，工具应提供默认值。
_Avoid_: 必填基础参数、精度保证、结果误差本身

**失败项** — 一次 **轨道族转换** 中，某条 **单条轨道** 未能成功转换时写入 **轨道族转换结果** 的记录。失败项属于转换结果内部的条目。
_Avoid_: 失败任务、部分成功、日志错误

**修正** — 为满足周期性、连续性或目标约束，对初始条件、轨迹或 patch points 做的数值调整。修正是数值方法概念；在具体工具中应通过限定词说明修正对象，例如轨道修正、周期轨道修正或 patch points 修正。
_Avoid_: 所有修正都是同一种算法、优化、失败后的手动改参数

### 可视化与分析

**可视化（Visualization）** — 纯输出/渲染层。接收 `Orbit`、`OrbitFamily`、`Transfer` 等数据，生成 2D/3D 图像或诊断图表。不修改输入数据，不持有任务状态。
_Avoid_: 绘图（当指代交互式查看时）

**绘图与可视化** — 将 **单条轨道**、**轨道族**、转换结果或分析数据展示为图形的工具类别。静态图像生成称为绘图；交互式查看或动态展示称为可视化。
_Avoid_: 只说绘图（无法覆盖未来交互式查看）、只说可视化（容易弱化静态图输出）

**检查与分析** — 用于验证 **输入文件** 或 **输出文件**、查看数值诊断、比较结果或理解轨道数据特征的工具类别。检查强调是否符合预期；分析强调解释数据特征。
_Avoid_: 只说检查（无法覆盖数据解释）、只说分析（容易弱化验证语义）

**坐标系** — **绘图与可视化** 中解释轨道状态/转换结果/绘图位置所用的参考框架（即 `系统与环境` 节定义的 **坐标系**）。仅影响数据含义与显示方式，不改变数值。
_Avoid_: 绘图样式、显示单位、主题

**图层** — **绘图与可视化** 中可独立显示、隐藏或调整样式的一类视觉元素，例如轨道曲线、天体图标、平动点、坐标轴或标注。图层只影响展示，不改变轨道数据、计算结果或输出文件中的数值内容。
_Avoid_: 数据筛选、删除数据、坐标系、输出文件内容

**标注** — **绘图与可视化** 中用于说明轨道、点、事件、参数或诊断结果的文本或符号。标注可以由工具自动生成，也可以由用户设置。
_Avoid_: 完整诊断信息、日志、数据本身、只指手写文字

**导出图像** — 用户将当前 **绘图与可视化** 结果保存为图像文件的操作。导出的图像文件属于输出文件，但只保存可视化结果，不代表重新生成或修改轨道数据。
_Avoid_: 导出轨道数据、重新生成轨道、修改输入文件

**诊断信息** — 工具写入输出文件或转换结果中的质量指标、收敛信息、失败原因或数值检查结果。诊断信息用于解释计算质量、复查失败原因或比较不同结果。
_Avoid_: 临时日志、界面提示、用户备注

**工作流链路** — 用户将一个工具产生的 **输出文件** 作为后续工具的 **输入文件**，逐步完成生成、转换、转移设计、绘图、检查与分析等目标的使用过程。
_Avoid_: 工具链（容易强调实现组合）、处理流程（过于泛化）、隐式自动串联

## 关键约定

| 约定 | 规则 |
|------|------|
| 状态向量顺序 | `[x, y, z, vx, vy, vz]` — 不可变更 |
| 状态序列形状 | `(n_points, 6)` — 不是 `(6, n_points)` |
| STM 形状 | `(n_points, 6, 6)` |
| 数值积分容差 | `rtol=atol=1e-12`（默认） |
| 坐标系与单位 | `Orbit` 状态由绑定的 `System` 解释（`frame` 标识 / `coordinate_system` 变换 / `unit_system` 量纲） |
| 参考历元 | `"2025-06-21T11:00:06"`（SPICE 操作默认） |

## 领域关系

- 一个 **轨道族** 包含一条或多条 **单条轨道**。
- **轨道族转换** 对每条 **单条轨道** 执行独立的星历模型转换。
- 每次星历转换从一个 **星历转换输入文件** 加载 **单条轨道** 或 **轨道族**。
- 每次星历转换使用一个 **星历转换方法** 构造并修正星历模型中的轨迹表示；分段星历转换可构造和修正 **patch points**，不分段星历转换可构造和修正 **轨道初值**。
- 每次星历转换必须由用户提供一个 **参考历元**。
- 一次 **轨道族转换** 产生一个 **轨道族转换结果**。
- 共振比参数决定 **RO**（共振轨道）族的具体共振形式（3:1 或 3:2）。
- **参考历元** 决定 **待转换轨道** 的初始状态、**轨道初值** 或 **patch points** 如何映射到 J2000 星历时间。
- **绘图与可视化** 围绕 **坐标系**、**图层**、**标注** 和 **导出图像** 等概念组织。

## 已解决的歧义

- "一个轨道族转换"曾可能表示沿族递推或只转换代表性轨道；已解析为逐条独立转换完整族。
- DRO（原始含义 Direct Prograde & Retrograde Orbits）与 Distant Retrograde Orbit 重名；已拆分为 DRO（Distant Retrograde Orbit）和 DPO（Direct Prograde Orbit）两个独立族。
- RO（Resonant Orbit，共振轨道）通过共振比区分 3:1 和 3:2 共振比例。
- "输入文件"曾可能被理解为工具自动选择最新输出文件；已解析为用户为工具显式选择的文件。

## 待决定

以下概念尚未在代码和模型中定型，后续讨论后补充：

- **延拓策略抽象**：`Continuation` 与单步问题结构（自由变量、约束）如何分离。
- **MBSE 基础设施**：需求追踪、组件注册与图表生成的职责边界。
