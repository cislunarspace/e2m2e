# Context: e2m2e

Earth to Moon, Moon to Earth — 用于地月空间轨道与转移轨道设计的 Python 库。

## 这个库做什么

e2m2e 帮助设计地月空间中的轨道。典型流程是：先选一种动力学模型，再生成或修正一条轨道，必要时把它转换到更精确的星历模型里，最后画图检查。

库本身只负责这些计算步骤。它不处理航天器工程细节，也不做任务调度或姿轨控闭环仿真。

更具体地说，e2m2e 提供：
- 天体系统的动力学模型；
- 周期/拟周期轨道的生成与精化；
- 轨道之间的转移轨迹构造；
- CR3BP 轨道到星历模型的转换；
- 计算结果的可视化与分析。

## 领域术语

### 轨道与状态

**状态（State）** — 航天器在某一时刻的瞬时状态，即六元素数组 `[x, y, z, vx, vy, vz]`。在 e2m2e 中保持为 numpy 数组，形状 `(6,)`；作为 `Orbit.states` 中的一行时，就是该轨道在对应时刻的状态。状态向量的分量顺序固定不变。
_Avoid_: state（单独使用含混）

**单条轨道（Orbit）** — 航天器状态随时间演化的轨迹容器。持有状态序列 `states`（形状 `(n_points, 6)`）、时间序列 `times`（形状 `(n_points,)`）和绑定的 `system`。坐标系与单位由 `system` 解释。`Orbit` 不自带周期、Jacobi 常数、稳定性等派生属性，这些由外部算法按需计算。
_Avoid_: 单个 JSON、种子轨道（除非确实用于延拓）

**轨道族（Orbit Family）** — 同一类型、由连续参数（如 Jacobi 常数、振幅）索引的一组 `Orbit` 的集合。类型由 `OrbitFamilyType` 枚举标记。轨道族是生成的结果，不是生成它的方法；生成方法可以是延拓、微分修正或初始猜测策略。
_Avoid_: 采样轨道、代表性轨道集

**CR3BP 周期轨道族类型** — 以下轨道族在 e2m2e 中有定义：

| 族名 | 相关平动点 | 物理特征 | 实现状态 |
|------|------------|----------|----------|
| Lyapunov | L1, L2, L3 | 平面周期轨道，沿共线平动点主轴振荡 | 已实现 |
| Halo | L1, L2 | 三维周期轨道；高振幅区近直线晕轨道（NRHO） | 已实现 |
| Vertical | L1–L5 | 垂直方向振荡的周期轨道 | 已实现 |
| Axial | L1–L5 | 沿平动点轴向的周期轨道 | 待实现 |
| Butterfly | L1, L2 | 连接两个共线平动点的对称轨道 | 已实现 |
| Dragonfly | L1, L2 | 连接两个共线平动点的非对称轨道 | 已实现 |
| DRO | secondary | 围绕次天体的远程逆行轨道 | 已实现 |
| DPO | secondary | 围绕次天体的顺行轨道 | 待实现 |
| SPO | L4, L5 | 三角平动点附近的短周期轨道 | 待实现 |
| LPO | L4, L5 | 三角平动点附近的长周期轨道 | 待实现 |
| Tadpole | L4, L5 | 围绕单个三角平动点的蝌蚪形轨道 | 待实现 |
| Horseshoe | L4–L5 | 跨越两个三角平动点的马蹄形轨道 | 待实现 |
| RO | 全系统 | 满足 m:n 共振比例的周期轨道 | 已实现 |

**周期** — 一条 **单条轨道** 在 CR3BP 模型中完成一次重复运动所需的时间。周期可以来自用户输入、参考初值、轨道修正结果或输出诊断。周期不同于采样点数、传播时长。
_Avoid_: 采样点数、任意传播时长、周期倍数

### 轨道生成与分析

**初始条件（Initial Condition）** — 用户为生成轨道提供的起点信息，用于确定待生成轨道的初始状态或目标特征。初始条件可以来自手动输入，也可以来自 **参考初值**。
_Avoid_: 只指手动输入、排除参考初值、生成结果

**参考初值（Reference Initial Condition）** — 用户从 **参考数据集** 中选择的一条轨道记录，或按 Jacobi 常数等条件匹配得到的一组初始条件，用作生成工具的起点。启用参考初值后，与它对应的手动初始条件输入应互斥。
_Avoid_: 参考数据集（数据集合层级）、Catalog 种子、生成结果、手动输入值

**参考数据集（Reference Catalog）** — 项目提供或用户指定的轨道参考数据集合，包含可供生成工具使用的轨道记录。
_Avoid_: 参考目录（容易误解为文件夹目录）、数据库（容易误解为联网或 SQL 数据库）、模板

**初始猜测策略（Initial Guess Strategy）** — 为特定轨道族生成粗糙 `Orbit` 的策略函数，作为微分修正或延拓的输入。例如 Halo 轨道的三阶解析近似。
_Avoid_: 种子、参考轨道

**微分修正（Differential Correction）** — 单点初值修正算法。通过调整单个初始状态的自由分量，使后续轨迹满足约束条件（如周期性、对称性）。
_Avoid_: 优化、迭代求解

**修正策略（Correction Strategy）** — 微分修正的配置：定义固定参数、自由变量、目标约束及其权重。不同轨道族或对称性对应不同策略。
_Avoid_: 配置、参数

**多重打靶（Multiple Shooting）** — 将长轨迹拆分为多段，在每段端点施加连续性约束，从而降低积分敏感性、提高收敛性的边界值求解器。
_Avoid_: 分段积分、拼接

**延拓（Continuation）** — 从一条已知轨道、参考初值或已收敛解出发，沿某个轨道族参数方向逐步生成相邻轨道的过程。延拓通常用于 **生成轨道族**；它描述生成方法，不等同于 **轨道族** 本身。
_Avoid_: 轨道族本身、批量运行

**稳定性分析（Stability Analysis）** — 计算单周期轨道单值矩阵的特征值谱，以分类轨道稳定性。属于外部算法，结果不写入 `Orbit`。
_Avoid_: 特征值计算、稳定性判断

### 标准形（Normal Form）

平动点附近的轨道作周期或拟周期运动，但运动方程里的非线性项会让一条本该重复的轨道逐渐漂移、失稳。**标准形化简（Normal Form）** 的做法是：通过一连串坐标变换，把这些非线性耦合项逐层消去，直到轨道的动力学可以用少数几个几乎不变的参数来描述。它属于对已有轨道的分析方法，不生成新轨道。

e2m2e 把这件事做成一条流水线 `NormalFormPipeline`：输入一条 rho 坐标初值，依次经过动力学替代、quasi-Floquet 变换、中心流形化简，最终得到一组表征参数。上下文（平动点、历元、归一化单位、频率、特征指数）集中在 `NormalFormContext`，结果聚合在 `NormalFormResult`。

**rho 坐标系（平动点相对坐标系）** — 以某个平动点为原点的相对坐标系，状态为 6 维无量纲数组 `[ρ, ρ̇]`。标准形化简的所有后续步骤都在 rho 系里进行。它与"坐标系与单位"节的会合旋转系同源，只是原点平移到了平动点。
_Avoid_: 平动点坐标系（含混）、惯性系状态

**动力学替代轨道（dynamical substitute orbit）** — 严格的周期轨道只存在于理想 CR3BP；一旦加入太阳引力等摄动（星历模型），平动点附近不再有精确的周期解。动力学替代轨道是受摄系统里"最接近周期"的那条轨线，由多重打靶在一个时间窗口里修正到首尾连续闭合。它是流水线的第一站，由 `DynamicalSubstituteCorrector` 产出，挂在 `DynamicalSubstituteResult.substitute_orbit`。（该结果里的 `Kamiltonian` 字段是 qiao 的历史遗留命名，指变换后的 Hamiltonian，并非新概念。）
_Avoid_: 周期轨道（受摄系统里不再精确）、参考轨道

**生成函数 W（generating function W）** — 动力学替代步同时算出的、连接 rho 坐标与下一层（quasi-Floquet）坐标的近恒等变换的母函数，挂在 `DynamicalSubstituteResult.W_poly`。
_Avoid_: 把 W 当作轨道本身

**quasi-Floquet 变换矩阵 B(t)（quasi-Floquet transformation）** — 平衡点附近的线性化是常系数的，可以直接做 Floquet 分解；但替代轨道本身随时间运动，邻域的线性化是时变的。quasi-Floquet 变换用一个时变、保辛的矩阵 B(t)，把这条时变线性化系统化成常系数的实标准形（一个双曲方向加两个中心振荡方向），辛约束 `BᵀJB = J` 保证变换不破坏系统的哈密顿结构。由 `QuasiFloquetReducer` 求解，结果在 `QuasiFloquetResult`（`B_samples`、实标准形 `D`）。
_Avoid_: 普通 Floquet 变换（那是常系数情形）、状态转移矩阵

**中心流形（center manifold）** — 平动点附近有一个双曲（不稳定）方向和两个中心（振荡）方向。双曲方向让扰动指数增长，是轨道失稳的根源；中心方向上的运动有界。消去双曲方向与中心方向的耦合之后、只剩中心运动的不变流形就是中心流形——其上轨道不沿双曲方向逃逸，适合用作用量-角变量描述。`CenterManifoldReducer` 用高阶 Lie 变换做这步消去，各阶生成函数系数在 `CenterManifoldResult.W_series`。
_Avoid_: 把中心流形等同于整条轨道、稳定流形/不稳定流形（那是双曲方向的伴生流形）

**表征参数（representation parameters / param）** — 标准形化简的终点产物：一组作用量-角变量形式的 6 维坐标 `(q1, p1, I2, θ2, I3, θ3)`。理想情况下它们是运动积分（不随时间变化），于是可以用几个常数描述一条本需六个状态量随时间演化的轨道。rho 坐标与表征参数互为正逆变换，变换链为 `rho → EM → DS → QF → CM → param`，由 `LibrationCatalogTransformer` 的 `rho_to_param` / `param_to_rho` 提供。
_Avoid_: 把表征参数当作状态向量（它是化简后的约化坐标）、轨道初值

### 动力学模型

**动力学系统（System）** — 描述天体几何、引力与运动学模型，是后续计算的上下文。一个系统要回答四件事：
1. 用什么坐标框架谈论位置与速度？由 `frame` 标识。
2. 状态在不同坐标系之间怎么转换？由 `coordinate_system` 负责。
3. 数值用什么单位解释？由 `unit_system` 决定。
4. 各天体的引力参数是多少？通过 `gravitational_parameter(body)` 查询。
_Avoid_: 系统模型、动力学模型

**CR3BP 模型（CR3BP System）** — `System` 的实现，描述圆形限制性三体问题：两个主天体绕公共质心作圆周运动，第三体质量可忽略。采用以质心为原点的旋转坐标系，所有量无量纲化（DU, TU, VU）。在该会合旋转系中，运动按**相位**演化——给定初始相位，后续状态唯一确定，不必知道真实历元。这种不依赖于真实时间的系统称为**自治系统**。特有概念：`LibrationPoint`（平动点）。
_Avoid_: 三体系统、无量纲系统

**星历模型（Ephemeris System）** — `System` 的实现，基于 SPICE 内核查询天体星历。采用 J2000 等惯性坐标系，单位为物理单位（km, s, km/s）。运动按**历元**演化——天体在某一时刻的位置必须查星历才能确定，系统显式依赖于时间。这种系统称为**非自治系统**。不定义平动点。
_Avoid_: 星历数据源、目标天体历表

**动力学（Dynamics）** — 在指定 `System` 上积分运动方程、得到状态历史的对象。它回答：给定一个动力学模型，状态随时间怎么演化。

e2m2e 中的动力学实现，对应到不同模型下的动力学方程：
- **CR3BP 模型下的动力学方程**：在会合旋转系中积分，加速度由解析的伪势与科氏力给出。
- **力模型组合**：由多个力模型组合而成的动力学方程，在惯性系中积分。这是星历模型下推荐的动力学实现。

`EphemerisDynamics`（解析 N 体公式）是遗留实现，仅供 `multiple_shooting` / `homotopy` 内部使用（它们依赖其 STM 与 Jacobian，`ForceModel` 尚未补齐这两项）。新代码应使用 **力模型组合**（`ForceModel`）做星历外推，不直接使用 `EphemerisDynamics`。

_Avoid_: 求解器；把“解析 vs 力分解”当作根本差别（根本差别在坐标系）

**运动方程（equations of motion）** — 描述运动学参数（位置、速度）与**时间**关系的方程，即状态的运动学演化（位置的时间导数等于速度，dr/dt = v）。它回答"状态怎样随时间推进"，不涉及力。
_Avoid_: 与动力学方程混用（运动方程对时间，动力学方程对力）

**动力学方程（dynamical equations）** — 描述运动学参数与**力**关系的方程，一般为微分式（加速度由力求出，dv/dt = F/m）。它回答"力如何改变运动"。完整的运动方程组 = 运动方程（运动学部分）+ 动力学方程（动力学部分）；`动力学` 积分的就是这二者合成的状态导数 ODE。
_Avoid_: 与运动方程混用

**传播（propagation）** — 把运动方程在计算系中积分推进、得到状态历史的整个过程。它是 `动力学`（`Dynamics`）的职责：`Dynamics.propagate` 负责步长控制、（可选的）事件检测、状态与 STM 的提取，调用底层 `积分器` 完成单步推进。传播描述"做什么"（推进状态），不规定"怎么推一步"（那是积分）。
_Avoid_: 把传播等同于单步积分；把传播与积分当同义词

**积分（integration）** — `积分器` 执行**单步**数值推进的底层动作：给定当前状态与导数，推进一步并返回误差估计与步长建议。积分不持有 `System` 上下文、不做事件检测、不控制传播全程——那些是 `动力学` 的职责。传播调用积分，一步步积出整段轨迹。
_Avoid_: 把积分等同于传播全程（积分是单步，传播是全程）

**状态转移矩阵（STM / State Transition Matrix）** — 状态扰动的线性传播矩阵，形状 `(n_points, 6, 6)`。由 `Dynamics.propagate(..., with_stm=True)` 返回。
_Avoid_: 雅可比矩阵（STM 是状态空间的雅可比，但与约束雅可比不同）

**积分器（Integrator）** — `动力学` 调用的底层数值推进引擎，实现在 Rust crate（`crates/e2m2e-integrators`）并经 `e2m2e.integrators` 公开。共性：不持有 `System` 上下文、不做事件检测、不控制传播全程（那些是 `动力学` 的职责）；每次调用推进一步并返回误差估计与步长建议。分三族：
- **单步积分器（Single-step / Runge-Kutta 族）**：无状态、自适应步长、积一阶 `y'=f(t,y)`、输出全状态。`rk_step`，方法 `Pd45`/`Pd78`/`Rk89`。
- **多步积分器（Multistep / Adams 族）**：携带**历史缓冲**（纯导数采样）、固定步长、积一阶 `y'=f`、输出全状态。`multistep_step`，方法 `Abm`。
- **二阶积分器（Second-order / Cowell 族）**：携带**历史缓冲**（位置+加速度采样，采样格式与 Adams 不兼容）、固定步长、积二阶 `x''=a(t,x)`、输出**仅位置**（速度需有限差分恢复）。`cowell_step`。
_Avoid_: 步进器（overloaded；中文中可接受"步进器"作为实现层面同义词）；把多步/二阶排除在"积分器"外（与 GMAT 分类、crate 目录相悖）

**历史缓冲（history）** — 多步与二阶积分器跨步保留的既往采样（单步积分器无此物）。固定步长；改步长须重建。两种**不兼容**的采样格式：**Adams 族**为纯导数采样 `[f_{n-k}, …, f_n]`；**Cowell 族**为位置+加速度混合采样（如 `[x_{n-1}, x_n, a_{n-7}, …, a_n]`），故两者不能共用同一缓冲。
_Avoid_: 把 Adams 与 Cowell 的 history 视为同一种采样格式

**中间模型（Intermediate Model）** — 按贴近真实力模型的程度，模型分四级：**二体模型**（最简）< **CR3BP 模型**（三体轨道动力学最基础的模型）< **中间模型** < **高精度星历模型**（最接近真实）。中间模型是在 `CR3BP 模型` 基础上做了部分改进——声明摄动 `力模型`（如会合旋转系 + 太阳引力 + SRP）——构成的模型，**不是第三种基础 `System`**。加入时变力（太阳在会合系中运动）后，方程显式含时间，变为时间周期系统，不再自治。当前未实现。
_Avoid_: 把中间模型当作独立的第三种 System；把"CR3BP + 力"仍当作自治 CR3BP；以星历模型为基础谈"中间模型"（星历已是最接近真实的模型，其上无"中间"）

### 坐标系与单位

**坐标轴（`Axes`）** — 坐标系的"朝向"部分。它规定：同一个空间方向，在我的坐标系里如何表示，在 ICRF/J2000 里又如何表示。精确定义：给定历元 `et`，返回旋转矩阵 `R`，使得 `r_icrf = R @ r_axes`；以及（如果需要）旋转矩阵的变化率 `Rdot`，使得 `v_icrf = R @ v_axes + Rdot @ r_axes`。实现时按精度分档：SPICE ITRF93、GMAT 兼容原生、低精度教学、惯性 ICRF 系（单位阵）、IAU2000 春分赤道系等。
_Avoid_: 坐标系（指 `CoordinateSystem` 整体，非朝向部分）

**动态坐标轴（Dynamic Axes）** — 旋转矩阵不仅依赖历元，还依赖航天器瞬时状态的坐标轴。与静态坐标轴（ICRS、ITRF 等）不同，动态坐标轴的方向随航天器运动实时变化。实现上继承 `Axes` 但增加 `update(et, state)` 方法：先以状态刷新内部方向缓存，再调用 `rotation_matrix(et)` 取矩阵。状态注入由 `System.update_coordinate_systems` 或 `ForceModel.propagate` 在系统层面统一完成，避免多个力模型重复计算或状态不一致。
_Avoid_: 把动态坐标轴当作普通 `Axes` 使用（必须先 `update`）、在力模型内部各自更新

**VNB（Velocity-Normal-Binormal）** — 一种动态坐标轴，x 轴沿速度方向，z 轴沿轨道角动量方向，y 轴由叉积补全。用于推力方向、机动分解等需要"沿速度/垂直轨道面"表达的场合。轴向定义与 GMAT 一致。
_Avoid_: 与文献中其他 VNB 定义混用（如把 Normal 当作速度垂直于轨道面内的分量）

**LVLH（Local Vertical Local Horizontal）** — 一种动态坐标轴，z 轴沿位置反方向（从航天器指向地心，即 Local Vertical），y 轴沿负角动量方向，x 轴由叉积补全（即 Local Horizontal）。用于轨道保持、相对导航等场合。注意不同文献对 LVLH 的 x/y 命名有分歧，e2m2e 采用 GMAT 约定。
_Avoid_: 与文献中 z 轴为 `+r` 的 LVLH 定义混用

**原点（`Origin`）** — 坐标系的"位置"部分。`state(et)` 返回该原点在 ICRF/J2000 中的绝对状态 `[r, v]`。例如天体中心、质心等无平移点。
_Avoid_: 物理天体本身（原点是参考点）

**坐标系（`CoordinateSystem`）** — 一个 `坐标轴` 加一个 `原点` 拼成的完整数学参考框架，描述位置、速度、姿态、受力等矢量。状态转换时，先按原点的运动做平移，再按坐标轴的旋转做朝向变换。坐标系是"框架"，本身不限定用途：被选作描述运动参数的基准时称为 **参考系**，被选作积分计算的执行框架时称为 **计算系**。
_Avoid_: 坐标框架（口语同义词，规范用"坐标系"）；把 `ReferenceFrame` 枚举当坐标系（那是参考系的名称，不做变换）

**参考系** — 被选作"描述运动参数基准"的坐标系。它回答运动参数相对于哪个基准表达。在 e2m2e 中，状态存放与表达的坐标系（`system.coordinate_system`）承担参考系角色。力模型返回的加速度也在参考系下。
_Avoid_: 把参考系当成与坐标系并列的另一类对象（它是坐标系的角色）；把 `ReferenceFrame` 枚举叫参考系（枚举是名称，参考系是角色）

**计算系** — 积分（对运动方程做数值推进）所发生的坐标系。它回答积分在哪个坐标系里执行。参考系与计算系可以是同一个坐标系，也可以不同；在 e2m2e 中二者重合——`Dynamics.propagate` 在 `system.coordinate_system` 中积分，该系既作参考系又作计算系。理解计算系要与 **积分**、**传播** 放在一起：传播是把运动方程在计算系中积分推进的过程，积分器是执行单步的数值引擎。单个力为算自身加速度而临时变换到的坐标系（如阻力用 ITRF、光压用 ICRF）不是计算系——力算完即转回参考系，积分仍发生在参考系/计算系中。
_Avoid_: 把力计算用的坐标系当作计算系；因当前参考系与计算系重合就把二者当同义词混用（角色不同）

**参考系的名称（`ReferenceFrame` 枚举）** — 命名一个参考坐标系的标签，如 `J2000`、`ICRF`、`ITRF93`。它只标识"是哪个参考系"，不做坐标变换；变换由 `CoordinateSystem`（轴+原点）完成。
_Avoid_: 把 `ReferenceFrame` 当作坐标系本身或参考系角色（它只是名称标签）

**单位系统（Unit System）** — 描述数值的量纲，如 CR3BP 无量纲单位、SI 单位。同一坐标系中的状态可用不同单位系统表示。
_Avoid_: 单位、量纲

**平动点（Libration Point）** — CR3BP 模型中旋转坐标系下的五个平衡解：L1、L2、L3 共线，L4、L5 三角。仅属于 CR3BP 模型的概念。
_Avoid_: 拉格朗日点（虽然是同义词，但在 e2m2e 中用"平动点"）

### 力模型与传播

**力模型（`PhysicalModel`）** — 描述单一物理效应对航天器加速度的贡献。它接收时刻、状态和系统，返回在参考系下的加速度。引力场、大气阻力、太阳光压、连续推力等都是它的具体实现。部分力需在参考系之外的坐标系中计算（如阻力在 ITRF、光压在 ICRF），自行完成变换并把加速度转回参考系。
_Avoid_: 力（单独使用含混）、force（同上）

**中心引力（`PointMassGravity`）** — 把一个天体当作质点，算它对航天器的中心引力 `-μ r/|r|³`。`r` 是航天器相对该天体的位置；`μ` 由 `system.gravitational_parameter(body)` 查。它不查任何第三体的位置，因此只能表达 **参考系原点天体自身** 的引力（地心系里的地球、月心系里的月球）。要表达其他天体的引力贡献，用 **第三体引力**。
_Avoid_: 把 `PointMassGravity` 当作任意天体引力的通用工具（它只算中心引力，不含间接项）

**第三体引力（`ThirdBodyGravity`）** — 参考系原点之外的天体对航天器的引力摄动。一个实例对应一个摄动天体：`ThirdBodyGravity("MOON")`、`ThirdBodyGravity("SUN")`。加速度由直接项与间接项合成：`a = -μᵢ [(r - rᵢ)/|r - rᵢ|³ + rᵢ/|rᵢ|³]`，其中 `rᵢ` 是摄动天体相对 **参考系原点** 的位置（由 `system.get_body_position` 查 SPICE），`r` 是航天器相对原点的位置。间接项 `-μᵢ rᵢ/|rᵢ|³` 扣除摄动天体对原点的引力，以保持坐标原点固定在原点天体而非质心。原点取自 `system.origin`，不作为参数暴露——间接项只在"力原点 = 系统原点"时物理正确。在 `ForceModel` 里把 `PointMassGravity(原点天体)` 与若干个 `ThirdBodyGravity(摄动天体)` 组合，物理上等价于 `EphemerisDynamics` 的解析 N 体公式，应给出同一条轨迹。
_Avoid_: 第三体力（规范用"第三体引力"，与中心引力/球谐引力同属引力类）、点质量力（与 `PointMassGravity` 名近职异，易混）

**球谐引力场（`GravityField`）** — 把一个天体的引力场展开为完全正规化球谐系数（Cnm/Snm），用 Pines 递推计算非球形引力加速度。它 **天体无关**：地球（EGM96）、月球（GRGM900C）等任意天体共用同一个类，差别只在系数文件、GM、参考半径和 **body-fixed 轴**。一个实例绑定一个中心天体：`GravityField("EARTH", degree=10)`、`GravityField("MOON", degree=50, gravity_file="grgm900c.cof")`。它在天体固连系中展开球谐（地球 ITRF93、月球 MOON_PA），自行完成参考系↔固连系的坐标变换。`body` 参数驱动 body-fixed 轴的切换——这是天体无关的关键：不按 body 选对轴，球谐系数就会在错误的坐标系里展开。GM 和参考半径从引力场文件头读取（与天体无关的解析器同时支持 ICGEM `.gfc` 和 GMAT `.cof` 格式）。
_Avoid_: 把 `GravityField` 当作地球专用（它天体无关，按 body 切换轴）；把 body-fixed 轴选择当作用户职责（库按 body 自动选择）

**天体固连系（body-fixed frame）** — 固结在天体上、随天体自转的坐标系。球谐引力场在天体固连系中展开，因为球谐系数（Cnm/Snm）定义在天体固连框架下。不同天体的固连系实现不同：地球用 ITRF93（IAU 2000 CIO 方法，需 binary PCK），月球用 MOON_PA（DE421 principal axes 系，需月球 binary PCK + frame kernel）。`GravityField` 按 `body` 自动选择对应的固连系轴，用户不需手动指定。
_Avoid_: 把天体固连系当作惯性系（它是旋转系）；把不同天体的固连系混用（月球系数必须在 MOON_PA 展开，不能用 ITRF）

**固体潮（solid tide）** — 外部天体引力使中心天体弹性形变，进而改变其引力场系数（ΔC/ΔS）的效应。固体潮修正的 Step1 公式（IERS TN32 eqn.1）**天体无关**——对任意中心天体，传入扰动体列表（位置 + GM）+ Love number 即可累加。地球还有额外的频率相关项（Step2，IERS Table 6.3）、极潮和永久潮汐修正，这些是地球 IERS 规范专用的。月球固体潮用 Love number k₂=0.024116，扰动体是地球。
_Avoid_: 把固体潮等同于海洋潮汐（e2m2e 只做固体潮）；把地球专用的 Step2/极潮当作天体无关

**力模型组合（`ForceModel`）** — 把多个力模型组合成一次传播所需运动方程的对象。它决定哪些力模型启用，把它们产生的加速度合成总加速度，再写成状态导数形式。为了支持不同任务对力的不同选择，它也负责按名登记、启用/禁用和配置序列化。每个力模型自己处理所需的坐标变换；组合只汇总它们返回的加速度。由于它继承 `Dynamics`，也可以作为动力学实现直接使用。
_Avoid_: 力模型聚合、容器协调坐标转换（误导：暗示容器代劳变换）

**力模型配置（Force Model Config）** — 一组力模型的清单，写成 JSON 或 dict（是数据，不是代码）。每条写明一个力是什么、参数取多少、叫什么名字、是否启用。力模型组合读这份清单就能构建出来，不必逐条写 Python 调 `add_force`。配置只记力本身，不记初始状态或传播时长——那是运行时才给的。配置能存盘、也能原样读回；凡不能写成数据的输入（如手写的 Python 函数）配置管不了。
_Avoid_: 把配置当作一次完整传播的输入（配置只列力；初始状态、传播时长、积分参数是传播时另给的，不进配置）；把字段名、版本号这类序列化细节当作领域概念来记（那是实现，归代码与 ADR）

**传播中的推力/机动** — 航天器在传播过程中改变速度的事件。与转移设计中的推进模型不同，这里的推力/机动直接参与运动方程或传播流程。
- `FiniteBurn`：连续推力，作为力模型参与加速度计算。运行时 `thrust_profile` 与 `direction` 均可接受可调用对象；但配置往返（`to_config` / `from_config`）目前只支持固定推力剖面（`constant`、`pulse`）和固定方向向量（`fixed`），任意 Python callable 会导致 `NotSerializableError`。
- `ImpulsiveBurn`：瞬时 Δv 事件，在指定 epoch 处直接修改状态速度，不由力模型表达。

限制：当前 Δv 与方向只在传播（惯性）坐标系内解释，不做坐标变换；VNB/LVLH burn 坐标系推迟到 Slice 12。质量变化也不支持（`mass` 常量）。
_Avoid_: 推进方式（与转移中的推进模型混淆）、burn（单独使用含混，须带限定：脉冲/有限）

### 转移设计

**转移（Transfer）** — 从出发条件到到达条件的轨迹。出发与到达由 `TerminalCondition` 定义，不由 `Orbit` 强制限定。
_Avoid_: 轨道族转换、星历转换

**转移中的推进模型（Propulsion Model）** — 转移设计时用来建模出发/到达速度变化的策略。`Transfer` 持有推进模型以支持不同转移类型。它只关心两端点的速度分解，不参与传播过程。
- `ImpulsivePropulsion`：脉冲机动，速度瞬时变化；将出发速度分解为切向与法向分量：`v = alpha * |v| * t_hat + beta * |v| * n_hat`，其中 `alpha` 为切向速度比，`beta` 为法向速度比（默认 `0.0`，即纯切向）。
- `LowThrustPropulsion`：连续小推力（待实现）。
_Avoid_: 推进方式、动力模型

**端点条件（Terminal Condition）** — 转移出发或到达必须满足的条件。
- `OrbitTerminal`：位于某条 `Orbit` 上，出发状态取轨道首点，到达状态通过动力学传播获取。
- `StateTerminal`：固定状态与时间，出发与到达状态均为固定值，不依赖动力学传播。已实现，但尚未接入 DRO→RO 优化器。
- `LEOTerminal`、`GEOTerminal`：近地/地球同步停泊轨道端点（待实现）。
- `BodySurfaceTerminal`：位于天体表面某点（待实现）。
_Avoid_: 出发轨道、到达点（过于笼统）

**Δv（Delta-v）** — 速度增量，衡量转移所需的速度变化总量。脉冲模型中为各脉冲大小之和。
_Avoid_: 推力、燃料消耗

### 星历转换

**星历转换** — 将 CR3BP 轨道转换到星历模型的过程。星历转换不同于 **转移设计**：前者解决动力学模型转换问题，后者解决任务连接与设计问题。
_Avoid_: 转移设计、轨道族转换

**轨道族转换** — 将轨道族内每条轨道逐条独立转换到星历模型；单条转换失败时默认记录该项并继续处理其余轨道，可由用户选择快速失败。
_Avoid_: 沿族递推、只转换里程碑轨道

**星历转换输入文件** — 用户显式提供给转换工具的 CR3BP 轨道 JSON 文件；工具不应隐式选择最新文件。
_Avoid_: 最新输出文件、默认输入文件

**轨道族转换结果** — 一次 **轨道族转换** 产生的单个汇总 JSON，包含每条轨道的转换状态、诊断信息和成功时的星历轨迹。
_Avoid_: 每轨道分散输出、只保存成功项

**星历转换方法** — 星历转换工具用于构造并修正星历模型中轨迹表示的算法选择。分段表示时，它修正各 **patch points** 的 **轨道初值**，使每段外推到下一个 patch point 时位置、速度连续。不拆分时，它直接构造并修正整条轨道的初值。
_Avoid_: 星历数据源、目标天体历表、固定不可选算法

**参考历元** — 用户为星历转换显式提供的 UTC 时间基准。它把 CR3BP 轨道的初始状态、**轨道初值** 或 **patch points** 映射到 J2000 星历时间。同一份 **星历转换输入文件** 在不同参考历元下可能得到不同结果。
_Avoid_: 隐式默认历元、轨道周期、普通运行时间

**待转换轨道** — 星历转换从 **星历转换输入文件** 中读取并准备转换到星历模型的 **单条轨道**。输入文件包含 **轨道族** 时，族中每条单条轨道都是一个待转换轨道，按轨道族转换语义逐条独立转换。
_Avoid_: 输入文件本身、最新输出文件、轨道族整体作为一个不可拆分对象

**patch points** — 分段星历转换中用于表示轨迹段之间连接关系，并进行连续性修正的轨迹连接点。不是所有星历转换都需要 patch points；对于不拆分的轨道，工具可以只构造和修正轨道初值。
_Avoid_: 所有星历转换都必须有的点、轨道初值、采样点、标注点、目标点

**容差** — 工具用于判断匹配、收敛、连续性或误差是否可接受的数值阈值。容差通常属于高级参数，工具应提供默认值。
_Avoid_: 必填基础参数、精度保证、结果误差本身

**失败项** — 一次 **轨道族转换** 中，某条 **单条轨道** 未能成功转换时写入 **轨道族转换结果** 的记录。失败项属于转换结果内部的条目。
_Avoid_: 失败任务、部分成功、日志错误

**修正** — 为满足周期性、连续性或目标约束，对初始条件、轨迹或 patch points 做的数值调整。修正是数值方法概念。具体工具中应通过限定词说明修正对象，例如轨道修正、周期轨道修正或 patch points 修正。
_Avoid_: 所有修正都是同一种算法、优化、失败后的手动改参数

### 可视化与分析

**可视化（Visualization）** — 把 `Orbit`、`OrbitFamily`、`Transfer` 等数据画成图或图表的环节。它不修改输入数据，也不持有任务状态。
_Avoid_: 绘图（当指代交互式查看时）

**绘图与可视化** — 将 **单条轨道**、**轨道族**、转换结果或分析数据展示为图形的工具类别。静态图像生成称为绘图；交互式查看或动态展示称为可视化。
_Avoid_: 只说绘图（无法覆盖未来交互式查看）、只说可视化（容易弱化静态图输出）

**检查与分析** — 用于验证 **输入文件** 或 **输出文件**、查看数值诊断、比较结果或理解轨道数据特征的工具类别。检查强调是否符合预期；分析强调解释数据特征。
_Avoid_: 只说检查（无法覆盖数据解释）、只说分析（容易弱化验证语义）

**坐标系** — **绘图与可视化** 中解释轨道状态/转换结果/绘图位置所用的参考框架（即 `坐标系与单位` 节定义的 **坐标系**）。仅影响数据含义与显示方式，不改变数值。
_Avoid_: 绘图样式、显示单位、主题

**图层** — **绘图与可视化** 中可独立显示、隐藏或调整样式的一类视觉元素，例如轨道曲线、天体图标、平动点、坐标轴或标注。图层只影响展示，不改变轨道数据、计算结果或输出文件中的数值内容。
_Avoid_: 数据筛选、删除数据、坐标系、输出文件内容

**标注** — **绘图与可视化** 中用于说明轨道、点、事件、参数或诊断结果的文本或符号。标注可以由工具自动生成，也可以由用户设置。
_Avoid_: 完整诊断信息、日志、数据本身、只指手写文字

**导出图像** — 用户将当前 **绘图与可视化** 结果保存为图像文件的操作。导出的图像文件属于输出文件，但只保存可视化结果，不代表重新生成或修改轨道数据。
_Avoid_: 导出轨道数据、重新生成轨道、修改输入文件

**诊断信息** — 工具写入输出文件或转换结果中的质量指标、收敛信息、失败原因或数值检查结果。诊断信息用于解释计算质量、复查失败原因或比较不同结果。
_Avoid_: 临时日志、界面提示、用户备注

**工作流链路** — 用户将一个工具的输出文件作为后续工具的输入文件，逐步完成生成、转换、转移设计、绘图、检查与分析等目标的过程。
_Avoid_: 工具链（容易强调实现组合）、处理流程（过于泛化）、隐式自动串联

## 关键约定

| 约定 | 规则 |
|------|------|
| 状态向量顺序 | `[x, y, z, vx, vy, vz]` — 不可变更 |
| 状态序列形状 | `(n_points, 6)` — 不是 `(6, n_points)` |
| STM 形状 | `(n_points, 6, 6)` |
| 数值积分容差 | `rtol=atol=1e-12`（默认） |
| 坐标系与单位 | `Orbit` 状态由绑定的 `System` 解释（`frame` 标识 / `coordinate_system` 变换 / `unit_system` 量纲） |
| 参考历元 | `"2025-06-21T11:00:06"`（SPICE 操作默认） |

## 领域关系

- 一个 **轨道族** 包含一条或多条 **单条轨道**。
- **轨道族转换** 对每条 **单条轨道** 执行独立的转换，得到 **星历模型** 下的轨迹。
- 每次星历转换从一个 **星历转换输入文件** 加载 **单条轨道** 或 **轨道族**。
- 每次星历转换使用一个 **星历转换方法** 构造并修正 **星历模型** 中的轨迹表示；分段星历转换可构造和修正 **patch points**，不分段星历转换可构造和修正 **轨道初值**。
- 每次星历转换必须由用户提供一个 **参考历元**。
- 一次 **轨道族转换** 产生一个 **轨道族转换结果**。
- 共振比参数决定 **RO**（共振轨道）族的具体共振形式（3:1 或 3:2）。
- **参考历元** 决定 **待转换轨道** 的初始状态、**轨道初值** 或 **patch points** 如何映射到 J2000 星历时间。
- **绘图与可视化** 围绕 **坐标系**、**图层**、**标注** 和 **导出图像** 等概念组织。

## 待决定

以下概念尚未在代码和模型中定型，后续讨论后补充：

- **延拓策略抽象**：`Continuation` 与单步问题结构（自由变量、约束）如何分离。
